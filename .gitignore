# Build artifacts
ucx-install/
rdu-ucx-install/
.venv_bench/
.venv_rdu/
.gpu_cache/
*.log
# snrdu auto-generated batch-script artifacts (job-name-based, no fixed prefix)
bash_[0-9]*

# Python
__pycache__/
*.pyc
*.pyo
*.pyd

# Wheels (fetch/build via build/)
wheelhouse/

# Results
benchmark_results/

# Runtime-only repos — cloned at setup time (see README.md)
vllm-rdu/
InferenceX/
# Reference-only checkout, kept around for comparison during the fast-coe ->
# vllm-rdu migration; not a build dependency.
fast-coe/
vendor/bin/
rdu-build-src/
bar2-build-src/
rdu-runtime-install/
gpu-build-src/

# Cluster-specific local notes
docs/local/
.docker-buildx/
# GPU node rdma-core libs (copied by build setup, node-specific binaries)
.rdma-libs/
