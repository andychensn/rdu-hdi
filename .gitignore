# Build artifacts
ucx-install/
rdu-ucx-install/
.venv_bench/
.venv_rdu/
.gpu_cache/
.rdu_cache/
.etcd_data/
.etcd_data2/
build/
*.log
# snrdu auto-generated batch-script artifacts (job-name-based, no fixed prefix)
bash_[0-9]*

# Python
__pycache__/
*.pyc
*.pyo
*.pyd

# Wheels (fetch/build via scripts/)
wheelhouse/

# Results
benchmark_results/

# Runtime-only repos — cloned at setup time (see README.md)
fast-coe/
InferenceX/
vendor/bin/
rdu-build-src/
bar2-build-src/
rdu-runtime-install/

# Cluster-specific local notes
docs/local/
.docker-buildx/
# GPU node rdma-core libs (copied by build setup, node-specific binaries)
.rdma-libs/
